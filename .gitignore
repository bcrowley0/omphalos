# ---- Secrets (NEVER commit) ----
.env
**/.env
!**/.env.example

# ---- Python (api/) ----
.venv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ---- Node / Next.js (web/) ----
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# ---- Editor / OS ----
.DS_Store
*.log
.idea/
.vscode/

# ---- Local dev convenience ----
# dev.sh and new-worktree.sh ARE committed (reproducible workspace setup).
# go.sh stays local — it's a personal tmux + `claude` launcher, not a project fact.
go.sh
dev-servers.log
.worktrees/

# IBKR OAuth RSA private keys — never commit
api/secrets/
